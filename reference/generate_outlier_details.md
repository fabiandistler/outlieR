# Generate Detailed Outlier Information

Create detailed outlier information showing which columns contribute to
each observation being classified as an outlier.

## Usage

``` r
generate_outlier_details(data, prep_data, scores, outliers, model, threshold)
```
